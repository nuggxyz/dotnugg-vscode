const main = async () => {
    // const text = fs.readFileSync('./test/Base.nugg', 'utf-8');
    // const tmp = await FileParser.init(text);
    // tmp.parse();
    // tmp.attrs[0].data.matrix.forEach((x) => {
    //     console.log(x.length);
    // });
    // console.log(tmp.attrs);
};

main();
